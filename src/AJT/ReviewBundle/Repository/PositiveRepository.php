<?php

namespace AJT\ReviewBundle\Repository;

use Doctrine\ORM\EntityRepository;
use Doctrine\ORM\Tools\Pagination\Paginator;

/**
 * PositiveRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class PositiveRepository extends EntityRepository
{
	public function getPaginatePositive($pageSize,$currentPage){
        $em=$this->getEntityManager();
        
        //DQL
        $dql = "SELECT positive FROM AJT\ReviewBundle\Entity\Positive positive ORDER BY positive.id ASC";
        $query = $em->createQuery($dql)
                               ->setFirstResult($pageSize * ($currentPage - 1))
                               ->setMaxResults($pageSize);
 
        $paginator = new Paginator($query, $fetchJoinCollection = true);
 
        return $paginator;
    }
}
